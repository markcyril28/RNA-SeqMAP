Organize this modules folder. 
Implement DRY principle. 
Put the important parameters and settings at the top of the scripts. 
Edit other scripts that may affected by the changes. 
Don't create any markdown files or lenghty explanation. Comments in the scripts suffice.
Always be concise in reporting in the chats.  

Folders: 
    0_input_information
        - contains the input files. 
    a_preprocessing
        - global_config_preproc.sh - global config within a a_preprocessing
        - shared_utils_preproc.sh - shared utils within a_preprocessing
        - download.sh 
        - trimming.sh 
        - quality_checks.sh 
    b_main_methods
        - global_config_methods.sh - global config within a_preprocessing
        - shared_utils_methods.sh 
        - separate the methods into each own bash scripts. 
        Don't write Rscripts within bash script. Create a separate helper Rscripts
    c_post_processing
        
    logging
        - logging_utils.sh

New Feature: GPU Use 
    First check if GPU exist. 
    If it exists, 
        use GPU to any operations that could benefit from GPU acceleration. 
    And also a script to prepare the GPU if still unprepared. 
        CUDA and other stuffs. 